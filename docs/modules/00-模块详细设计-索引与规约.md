# postern 模块详细设计 · 索引与规约

> 本目录是 workspace 各 crate 的模块级详细设计：在《详细设计文档》（尤其第八部分"领域范围与内涵"）的领域裁决之上，逐一展开每个 crate 的定位、职责、功能、边界、与相邻模块的交互细节、必守不变量。**纯设计，不含任何实现代码、阶段划分或进度状态。** 与上层文档冲突时，以《技术设计文档》的公理与《详细设计文档》第八部分的领域裁决为准。

## 模块清单

| crate | 承载领域（详细设计第八部分） | 二进制 | 模块文档 |
|---|---|---|---|
| `postern-core` | 8.1 领域核心模型、8.3 策略引擎 | —（库） | [01-postern-core.md](01-postern-core.md) |
| `postern-store` | 8.11 存储层 | —（库） | [02-postern-store.md](02-postern-store.md) |
| `postern-secrets` | 8.8 机密面 | —（库） | [03-postern-secrets.md](03-postern-secrets.md) |
| `postern-transports` | 8.7 传输 | —（库） | [04-postern-transports.md](04-postern-transports.md) |
| `postern-adapters` | 8.6 适配器 | —（库） | [05-postern-adapters.md](05-postern-adapters.md) |
| `postern-daemon` | 8.2 数据面内核、8.5 连接管理、8.9 观测面（执行）、8.10 控制面、8.12 外壳（服务端） | `posternd` | [06-postern-daemon.md](06-postern-daemon.md) |
| `postern-cli` | 8.12 外壳（客户端） | `postern` | [07-postern-cli.md](07-postern-cli.md) |

## 每篇模块文档的统一结构

每篇模块文档必须含以下小节，顺序一致：

1. **定位（一句话）** — 这个 crate 在系统中是什么。
2. **承载领域与职责范围** — 对应第八部分哪些领域；本 crate 负责什么（封闭列举）。
3. **支持的功能** — 该 crate 对外提供的能力清单（按对外接口组织）。
4. **明确边界（不做什么）** — 显式排除项，每项指明"归哪个 crate / 领域"。
5. **对外接口** — 该 crate 暴露给其他 crate 的类型与 trait（设计级签名，非实现）；标注哪些是定义、哪些是实现。
6. **与相邻模块的交互** — 对每个有依赖关系的相邻 crate，逐一写明：**方向**（谁调谁）、**内容**（传什么类型/数据）、**时机**（在求值管线/启动序列的哪一步）、**失败语义**（出错时如何 fail-closed）。
7. **必守不变量** — 该 crate 必须守住的规则，标注由哪条 Stele 契约 / clippy lint / 构造函数签名强制。

## 权威依赖图（唯一事实来源）

源自《详细设计文档》3.2，由契约 `ARCH_FORBIDDEN_EDGES` 强制。各模块文档的"交互"小节必须与此图一致，不得描述被禁止的依赖。

```
                 postern-core         ← 不依赖工作区内任何 crate（零 IO）
                /   |    |   \
       postern-store |  postern-transports  postern-adapters
                postern-secrets
                     \   |   /
                  postern-daemon       ← 唯一组装点（二进制 posternd）
                      |
                 postern-cli           ← 仅依赖 core（共享类型）+ HTTP/UDS 客户端
```

**允许的依赖边**：
- 所有 lib crate（store/secrets/transports/adapters）→ `core`（消费领域类型与插件 trait 定义）。
- `daemon` → core / store / secrets / transports / adapters（唯一组装点，可依赖全部）。
- `cli` → core（共享请求/响应/分页类型）+ HTTP-over-UDS 客户端依赖。

**禁止的依赖边**（违反即 `ARCH_FORBIDDEN_EDGES` 红）：
- `adapters` ↛ `secrets` / `transports` / `store`
- `transports` ↛ `store`
- `cli` ↛ `store` / `secrets`
- `core` ↛ 任何工作区 IO crate

## 跨模块交互矩阵（概览）

下表是各模块文档"交互"小节的总览；细节在各篇展开。"经由"列指交互所凭借的接口/类型。

| 调用方 | 被调方 | 经由 | 时机 |
|---|---|---|---|
| daemon::kernel | core::eval `Evaluator` | `evaluate(req, ci, constraint_check, snapshot, now)` | 求值步骤 [1][3][5][6] |
| daemon::kernel | adapters `Adapter` | `classify` / `check_constraint` / `execute` | 步骤 [2][4][8] |
| daemon::kernel | store `PolicyView` | `snapshot() -> Arc<PolicySnapshot>` | 每请求读快照（无锁） |
| daemon::kernel | daemon::connpool | `acquire(resource, tier) -> Channel` | 步骤 [7] |
| daemon::kernel | secrets `Sanitizer`/ScrubSet 句柄 | `scrub` / `scrub_stream` | 步骤 [9] 出口 |
| daemon::kernel | 观测 `AuditSink`（store 实现） | `record(event)` | 步骤 [7a]/[10] |
| daemon::connpool | secrets `CredentialProvider` + 映射解析 | `credential_for(res, tier)` / `resolve(code)` | 建立通路时一次性取不透明句柄 |
| daemon::connpool | transports `Transport` | `open(target, cred) -> Channel` | 建立通路 |
| daemon::control | store `PolicyRepo` | 事务读写 + 快照重建 | 每次策略变更 |
| daemon::control | adapters `Adapter::discover` | 触发能力面探测 | 资源接入/发现 |
| daemon::boot | secrets vault `MasterKeySource` | `obtain()` 解锁 | 启动序列（开放数据面前） |
| cli | daemon::control | HTTP/JSON over `control.sock` | 每条管理命令 |
| 外壳 shells | daemon::kernel | 提交 `NormalizedRequest` | 步骤 [0] 之后 |

## 通用约定

- 接口签名是**设计承诺**（与《详细设计文档》第四部分一致），实现可调内部细节但不得违背签名与不变量。
- 机密类型（`ResolvedTarget`/`ResourceCredential`/`PresentedCredential`）：不可 Clone/Serialize、`Debug=REDACTED`、无 `Display`；构造点受 `SEC_CONSTRUCTION_SITES` / `SEC_SECRET_TYPE_DISCIPLINE` 约束。
- 一切失败沿 fail-closed 解析为拒绝（公理二）；求值路径禁吞错（`EVAL_NO_ERROR_SWALLOWING`）。
- 文档语言：模块文档为中文；代码/标识符/接口名为英文（与上层一致）。
