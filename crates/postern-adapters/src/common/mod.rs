//! 各协议适配器共享的纯工具（骨架占位，无逻辑）。
//!
//! 本模块**不含任何协议语义**——只放跨协议复用的、与具体协议无关的小工具，
//! 当前唯一成员是对象引用规范化（[`object`]）：把各协议提取出的对象（schema.table /
//! `route:<path>` / `container:<名>`）统一规范化为 [`postern_core::request::ObjectRef`]，
//! 供 §3.2 细则判定与内核审计消费同一份对象视图（§3.1「对象供审计消费」）。
//!
//! 共享态纪律：本模块所有工具为**无内部可变共享态**的纯函数 / 不可变只读结构
//! （`Adapter: Send + Sync` 的并发安全靠「无共享可变态 + `&self`」而非锁，§3.7）。

pub mod object;
